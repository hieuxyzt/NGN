spring:
  application:
    name: eureka_server
server:
  port: 8761
eureka:
  instance:
    preferIpAddress: true
logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF
management:
  endpoints:
    web:
      exposure:
        include: info